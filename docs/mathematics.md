# The mathematics of the hypercube

Notes on the constructions used by this project. Everything here is
implemented in `src/core/` and verified by the tests in `test/`.

## 1. Construction — vertices as bit strings

The n-cube has `2^n` vertices. This project identifies each vertex with its
index `i ∈ [0, 2^n)`: bit `b` of `i` gives coordinate `b` as `−0.5` (unset)
or `+0.5` (set). Edge length is 1 and the circumradius is `√n / 2`.

The identification is load-bearing: two vertices are adjacent exactly when
their indices differ in one bit (Hamming distance 1), so the edge list is
generated by flipping each unset bit; a square face is a choice of two free
bits with the others fixed; a cell (facet) is the set of indices with one
bit fixed. The graph of vertices and edges is the hypercube graph `Q_n`.

## 2. Counting — C(n,k) · 2^(n−k)

A k-dimensional element of the n-cube is a choice of `k` free coordinates
(the element extends across them) and a fixed `±0.5` value for each of the
remaining `n−k`. Hence:

    count(k) = C(n,k) · 2^(n−k)

For `n = 4`: 16 vertices, 32 edges, 24 faces, 8 cells (and 1 tesseract).

Equivalently, every element corresponds to a string over `{−, 0, +}` per
coordinate (`0` marking a free axis), so the counts sum to `3^n` — an
invariant the test suite checks for `n = 1..7`.

## 3. Rotation in n dimensions

A rotation in n-space does not happen "around an axis" — that is a 3D
accident. A simple rotation fixes an `(n−2)`-dimensional subspace and mixes
the two coordinates of a plane. There are `C(n,2)` coordinate planes: 6 for
`n = 4` (xy, xz, xw, yz, yw, zw), 10 for `n = 5`, 15 for `n = 6` — the dot
grid in the UI is this formula made visible.

Two rotations in disjoint planes commute and can run simultaneously; with
equal angles in `xy` and `zw` this is an _isoclinic_ (Clifford) double
rotation, under which every vertex moves along a great circle of the
3-sphere — the `isocline` preset.

Plane rotations in general do **not** commute, so a pose cannot be stored
as a vector of angles: the same angles applied in a different order give a
different orientation, and a mouse drag composes arbitrary incremental
rotations onto the current pose. Orientation is therefore an accumulated
orthogonal matrix `Q ∈ SO(n)`, updated per frame by

    Q ← R(i, j, ω·dt) · Q

which touches only rows `i` and `j` (`applyPlaneRotation`, O(n²)).
Floating-point drift slowly de-orthogonalizes `Q`; a modified Gram–Schmidt
pass every ~300 frames restores `Q Qᵀ = I` (the drift test composes 10 000
rotations and checks exactly this).

## 4. Projection — the cascade nD → 3D → 2D

One projection stage consumes the last coordinate of a d-dimensional point:

    perspective:  p = v[0..d−2] · s,  s = D / (D − v[d−1])
    orthographic: p = v[0..d−2]

Applying stages repeatedly takes an n-dimensional object to the screen. The
coordinate consumed at the 3D→2D stage drives depth fading; the coordinate
consumed at the _first_ stage (the extra dimension) drives the violet–amber
temperature.

The three modes differ only in how the stages are configured:

- **perspective** — perspective division at every stage, camera at
  `1.2·√d` per stage (points stay within the circumradius `√d/2`, so
  nothing clips).
- **orthographic** — drop coordinates above 3D, keep perspective for
  3D→2D. A fully orthographic image reads flat; the mixed form is legible
  while still honest about the higher dimensions.
- **schlegel** — perspective from a viewpoint just outside one cell. The
  near cell projects large, the far cell nests inside it: the classic
  cube-within-a-cube diagram of the tesseract. The viewpoint distance is
  `max(w) + 0.35`, riding just outside the object: a fixed distance would
  diverge under rotation, when vertices sweep out to the circumradius and
  pass arbitrarily close to the viewpoint.

Beyond the three projections there is one alternative _view_: the **net**
(development) of the n-cube — its `2n` facet cells unfolded into the
hyperplane where the last coordinate is 0, and embedded back in `R^n` so
the same rotation and projection pipeline applies unchanged. For `n = 4`
this is the cross of eight cubes made famous by Dalí's _Corpus
Hypercubus_; for `n = 3` the Latin cross of six squares. Because the net
still lives in n-space, it can be rotated through the higher axes — the
flat cross waves through the fourth dimension.

## 5. Gray codes — a light that visits every vertex

The binary-reflected Gray code `g(i) = i XOR (i >> 1)` enumerates all `2^n`
indices so that consecutive values differ in exactly one bit — that is, it
is a Hamiltonian cycle on `Q_n`. The comet in the UI walks this cycle at
one vertex per second: a single point of light that visits every vertex of
the hypercube, changing one coordinate at a time, and returns home.

## 6. The symmetry group B_n

The full symmetry group of the n-cube is the hyperoctahedral group `B_n`:
every symmetry is a *signed permutation* — permute the axes (`n!` ways),
then flip the sign of any subset of them (`2^n` ways):

    |B_n| = 2^n · n!

384 for the tesseract. The group is generated by the axis mirrors together
with the quarter-turns in coordinate planes — which is exactly the grid in
the interface: the diagonal squares are the `n` mirrors, the dots above the
diagonal are the `C(n,2)` rotation planes.

A mirror cannot be reached by rotation (it reverses orientation), so the
interface animates it as a passage: the coordinate scales from `+1` through
`0` to `−1`, and the object collapses onto its `(n−1)`-dimensional shadow
before re-inflating reflected. Since the vertex set is setwise invariant
under every element of `B_n`, the geometry afterwards is unchanged — what
the reflection reverses is the motion, which continues mirror-imaged.
